# ROGII exp109 fast submission

Fast submission-only version of exp109. It uses the selected exp109 constants and skips the expensive local validation/grid-search audit, while still loading the same Kaggle competition input and artifact dataset and writing `/kaggle/working/submission.csv`.

Use this when you only need a leaderboard submission.
